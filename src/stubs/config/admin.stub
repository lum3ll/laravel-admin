<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Admin model location.
    |--------------------------------------------------------------------------
    |
    | Below will represent where the Admin model is in your current filesystem.
    | If you ever move it into a "models" directory for example, you will need
    | to update the namespace here.
    |
    */

    'location' => \App\Admin::class,

    /*
    |--------------------------------------------------------------------------
    | Admin route prefix.
    |--------------------------------------------------------------------------
    |
    | The below key is used to determine the route prefix when accessing
    | administrator routes. For example "/admin/dashboard".
    |
    */
    
    'prefix' => '/admin',

    /*
    |--------------------------------------------------------------------------
    | Admin authentication configuration.
    |--------------------------------------------------------------------------
    |
    | The below array represents the fields in your users table that will be 
    | used to validate a user against when logging into the admin backend.
    |
    */

    'authentication' => [
        'identifier' => 'email',
        'key' => 'password'
    ],

    /*
    |--------------------------------------------------------------------------
    | Admin table pagination.
    |--------------------------------------------------------------------------
    |
    | The below key will represent the pagination length when viewing
    | data from a given model. If you want to increase the data set you
    | can do that here.
    |
    */

    'pagination' => 20,

    /*
    |--------------------------------------------------------------------------
    | Admin model backends.
    |--------------------------------------------------------------------------
    |
    | The below array will be used to determine any tables that you want
    | to show in the laravel admin backend.
    |
    */

    'models' => [
        // 'users' => \App\User::class
    ],

];